[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0"
flask-sqlalchemy = ">=3.1"
flask-migrate = ">=4.0"
flask-jwt-extended = ">=4.0"
flask-cors = ">=4.0"
psycopg2-binary = ">=2.9"
python-dotenv = ">=1.0"
bcrypt = ">=4.0"

[dev-packages]

[requires]
python_version = "3.14"
